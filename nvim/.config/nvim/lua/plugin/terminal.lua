require 'toggleterm'.setup {
    shading_factor = 2,
    persist_size = false,
    open_mapping = [[<C-t>]],
    direction = 'float',
    float_opts = {
        border = 'curved',
        winblend = 0,
    },
}
