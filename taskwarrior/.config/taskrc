# [Created by task 2.5.3 5/21/2021 13:59:10]
# Taskwarrior program configuration file.
# For more documentation, see http://taskwarrior.org or try 'man task', 'man task-color',
# 'man task-sync' or 'man taskrc'

# Here is an example of entries that use the default, override and blank values
#   variable=foo   -- By specifying a value, this overrides the default
#   variable=      -- By specifying no value, this means no default
#   #variable=foo  -- By commenting out the line, or deleting it, this uses the default

# Use the command 'task show' to see all defaults and overrides

# General \ misc
data.location=~/.config/.task
weekstart=Monday
#include /usr/share/doc/task/rc/dark-violets-256.theme
#include /usr/share/doc/task/rc/dark-256.theme
include /usr/share/doc/task/rc/solarized-dark-256.theme
#include /usr/share/doc/task/rc/no-color.theme


# context
context.today.read=scheduled < tomorrow or due < tomorrow
context.today.write=scheduled:today

context.week.read=scheduled < 1week or due < 1week

context.work.read=project = Employment or +work
context.work.write=+work

context.easy.read=estimate < 4hour and difficulty = E

context.leisure.read=+joy or project = Art or +I


# UDA stands for User Defined Arguments
## Esimate — how much time will task occupy
uda.estimate.label=Estim
uda.estimate.type=duration

## Difficulty - how much of an effort task needs
uda.difficulty.type=string
uda.difficulty.label=⚡
uda.difficulty.values=,E,C,🪶,⚡  # easy, complex


# report
# report.next.columns = id,start.age,entry.age,depends,priority,project,tags,recur,scheduled.countdown,due.relative,until.remaining,description.count,urgency
report.next.labels=ID,Deps,Age,P,Due,Estim,⚡,Description,Sched,Project,Tag,Urg
report.next.columns=id,depends,entry.age,priority,due.relative,estimate,difficulty,description.count,scheduled.relative,project,tags,urgency
#report.next.sort=urgency-,priority+\/

## needs to be 
report.too_big.description=Pretenders to become projects
report.too_big.columns=id,priority,due.relative,estimate,description.count,project,tags
report.too_big.sort=estimate-
report.too_big.filter=status = pending estimate > 12h


# Urgency coefficients
#   Due + High priority = Top Urgency
#   Current project = Middle Urgency
#   Time Estimate matches my free time = Low Urgency
#   No priority, no estimate, no difficulty, etc. = is it even a task? should I keep it?
urgency.uda.difficulty..coefficient=-0.5
urgency.uda.estimate..coefficient=-0.8
urgency.uda.estimate.PT1H.coefficient=1  # short tasks = more priority
urgency.uda.estimate.PT30M.coefficient=3  # short tasks = more priority
urgency.uda.estimate.PT15M.coefficient=3  # short tasks = more priority
urgency.uda.estimate.PT5M.coefficient=7  # short tasks = more priority
urgency.user.tag.ZN.coefficient=0.8
urgency.uda.priority.L.coefficient=-2
urgency.blocking.coefficient=2
urgency.blocked.coefficient=-7
urgency.scheduled.coefficient=10


# taskwarrior-tui
uda.taskwarrior-tui.task-report.next.filter=status:pending


# End of file
# Below are 'task config'-setted values
news.version=2.6.0
